import AdminLayout from "@/layouts/AdminLayout";

const Overview = () => {
    return <div>Overview</div>;
};

Overview.layout = (page: JSX.Element) => <AdminLayout children={page} />;

export default Overview;
